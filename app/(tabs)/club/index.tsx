import { View, Text } from "react-native";
import React from "react";

type Props = {};

const MiClub = (props: Props) => {
  return (
    <View>
      <Text>MiClub</Text>
    </View>
  );
};

export default MiClub;
